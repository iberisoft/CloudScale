<UserControl x:Class="CloudScale.ScaleControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             DataContextChanged="UserControl_DataContextChanged" IsEnabled="False">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Label Grid.Row="0" Grid.Column="0" Content="Device ID:" />
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Path=DeviceId, Mode=OneWay}" IsReadOnly="True" />
        <Label Grid.Row="1" Grid.Column="0" Content="Resistance:" />
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Path=Resistance, Mode=OneWay}" IsReadOnly="True" />
        <Label Grid.Row="2" Grid.Column="0" Content="GPS:" />
        <UniformGrid Grid.Row="2" Grid.Column="1" Rows="1">
            <TextBox Text="{Binding Path=GlobalPosition[0], Mode=OneWay}" IsReadOnly="True" />
            <TextBox Text="{Binding Path=GlobalPosition[1], Mode=OneWay}" IsReadOnly="True" />
        </UniformGrid>
    </Grid>
</UserControl>
